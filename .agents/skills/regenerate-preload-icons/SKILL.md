---
name: regenerate-preload-icons
description: >
  Regenerate the generated icon sprite assets in the trizum PWA.
  Use after changing icon usage or adding/removing custom SVG files under src/icons.
  Triggers: "regenerate icons", "generate icon sprite", "update icon sprite",
  or when a new icon source needs to be reflected in the generated files.
---

# Regenerate Icon Sprite

`packages/pwa/src/generated/iconSprite.gen.ts` and
`packages/pwa/src/generated/iconSprite.svg` are generated by the PWA icon sprite
tooling. Regenerate them whenever icon usage changes or custom SVG sources
change.

## Steps

1. Regenerate the sprite assets:
   ```bash
   cd packages/pwa && pnpm icons:generate
   ```
2. Verify the changes in:
   - `packages/pwa/src/generated/iconSprite.gen.ts`
   - `packages/pwa/src/generated/iconSprite.svg`
3. Commit the updated generated files alongside the source changes that required
   them.
