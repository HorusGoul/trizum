---
"@trizum/pwa": patch
---

Improve the expense editor calculator so selected amounts are replaced on input, mobile auto-open avoids the native keyboard, attachments remain accessible, and browser back can close the calculator.
